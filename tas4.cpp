#include <iostream>
using namespace std;
int frequencyChecker(int number, int digit);
main()
{
    cout << "Enter a number: ";
    cin >> num;
    cout << "Enter the digit to check: ";
    cin >> d;
    frequencyChecker(num, d);
}
int frequencyChecker(int number, int digit)
{
    if (num % 100 = d)
}